# Never run dependency lifecycle scripts on a developer machine. CI and the
# release pipeline already install with --ignore-scripts; this makes a
# local `npm install` behave the same way, so a compromised dev
# dependency cannot execute code at install time. Explicit `npm run`
# targets (build, lint, test) are unaffected.
ignore-scripts=true
