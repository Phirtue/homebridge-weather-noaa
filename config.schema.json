{
  "pluginAlias": "NOAAWeather",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "NOAA Weather"
      },
      "station": {
        "title": "NOAA Station ID",
        "type": "string",
        "required": true
      },
      "refreshInterval": {
        "title": "Refresh Interval (minutes)",
        "type": "integer",
        "default": 15,
        "minimum": 5
      }
    }
  }
}